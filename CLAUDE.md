# Agent_Orchestration_View

Design documentation for this project is **deliberately kept out of git** and carried between
machines by hand. This repository holds code; it does not hold the design.

@docs/CONTEXT.md

## If `docs/CONTEXT.md` did not load

You are missing this project's design context — not because it does not exist, but because it has
not been copied onto this machine yet.

**Stop and ask the operator to copy `docs/` across before proposing or changing anything.**

Do not reconstruct the architecture from the code, and do not begin work on reasonable-looking
assumptions. Several of this project's decisions deliberately reject the conventional choice, so
sensible defaults are wrong here in ways that are not self-evident from the source.
